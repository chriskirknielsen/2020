<section>
	{% for post in postslist | reverse %}
		<article class="u-flow"{% if post.url == url %} data-current="current item"{% endif %}>
			<h3>
				<a href="{{ post.url | url }}">
					{% if post.data.title %}
						{{ post.data.title }}
					{% else %}
						Untitled
					{% endif %}
				</a>
			</h3>

			<p>
			{% if post.data.summary %}
				{{ post.data.summary }}
			{% else %}
				{{ post.excerpt }}
			{% endif %}
			</p>

			<p>
				<time datetime="{{ post.date | machineDate }}">
					<small>{{ post.date | readableDate }} by {{ metadata.author.name }}</small>
				</time>
			</p>

			{% if post.data.tags %}
				<ul>
					{% for tag in post.data.tags %}
						{%- if tag != "post" -%}
							<li>
								{% set tagUrl %}/tags/{{ tag }}/{% endset %}
								<a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
							</li>
						{%- endif -%}
					{% endfor %}
				</ul>
			{% endif %}
		</article>
	{% endfor %}
</section>
