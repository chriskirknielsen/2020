<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ navtitle or title or renderData.title }} | {{ metadata.title }}</title>
	{% if summary %}<meta name="description" content="{{ summary }}">{% endif %}
	<meta property="og:title" content="{{ navtitle or title or renderData.title }} | {{ metadata.title }}">
	{% if summary %}<meta name="og:description" content="{{ summary }}">{% endif %}
	<meta property="og:site_name" content="{{ metadata.title }}">
	<meta property="og:locale" content="{{ locale }}">
	<meta property="og:type" content="website">
	<meta property="og:url" content="{{ localePath | url | absoluteUrl(metadata.url) }}">
	<meta property="og:image" content="{{ '/assets/img/metaimage.jpg' | url | absoluteUrl(metadata.url) }}">
	
	<link rel="preload" href="{{ globalStyles }}" as="style">
	{%- set css -%}
	{%- include "assets/css/inline.css" -%}
	{%- endset %}
	<style>{{ css | cssmin | safe }}</style>
	{% set globalStyles = '/assets/css/global.css' | url %}
	<link rel="stylesheet" href="{{ globalStyles }}" media="all" id="global-styles">
	
	{# <link rel="preload" href="/assets/fonts/IBMPlexSansVar-Roman.woff2" as="font" type="font/woff2" crossorigin>
	<link rel="preload" href="/assets/fonts/IBMPlexSansVar-Italic.woff2" as="font" type="font/woff2" crossorigin> #}
	<link rel="preload" href="/assets/fonts/MarvinVisionsBig-Bold.woff2" as="font" type="font/woff2" crossorigin>

	{%- if ownstyles %}
		{% set ownStylesList = ownstyles | split(",") -%}
		{%- set pageStyleGroup = "" -%}
		{%- for ownStyle in ownStylesList -%}
			{%- set pagestylelink %}assets/css/{{ ownStyle }}.css{% endset -%}
			{%- set pagestyle %}{% include pagestylelink %}{% endset %}
			{%- set pageStyleGroup = pageStyleGroup + pagestyle -%}
		{%- endfor -%}
		<style>{{ pageStyleGroup | cssmin | safe }}</style>
	{%- endif -%}

	{% set js -%}
	{%- include "assets/js/inline.js" -%}
	{%- endset %}
	<script>{{ js | jsmin | safe }}</script>

	<link rel="icon" href="{{ '/assets/img/icon.png' | url }}" type="image/png">
	<link rel="icon" href="{{ '/assets/img/favicon.svg' | url }}">
	<link rel="mask-icon" href="{{ '/assets/img/mask-icon.svg' | url }}" color="{{ tokens.colors.dusk.secondary.med }}">
	<link rel="apple-touch-icon" href="{{ '/assets/img/apple-touch-icon.png' | url }}">
	<meta name="theme-color" content="{{ tokens.colors.dusk.primary.max }}">
	<link rel="manifest" href="{{ '/manifest.json' | url }}">
	
	{% if translationKey and section -%}
	{%- set resourceVariants %}{% if section == 'post' %}posts_all{% else %}pages_all{% endif %}{% endset -%}
	{%- set filteredVariants = collections | findVariants(resourceVariants, translationKey, locale) -%}
	{%- for variant in filteredVariants %}
	<link rel="alternate" href="{{ variant.url | url }}" hreflang="{{ variant.data.locale }}">
	{% endfor %}
	{%- endif -%}
	<link rel="alternate" type="application/rss+xml" title="{{ metadata.title }}" href="{{ localeRssUrl }}">
	{# <script src="{{ '/assets/js/color-scheme-switcher.js' | url }}" deferred></script> #}
</head>
